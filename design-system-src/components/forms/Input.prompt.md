Labeled text input; `mono` for clinical identifiers/values; `suffix` for units.

```jsx
<Input label="Blood pressure" mono suffix="mmHg" placeholder="120/80" />
<Input label="MRN" mono error="No matching record" />
```
