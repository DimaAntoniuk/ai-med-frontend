Action trigger; use `ai`/`aiSubtle` variants only for AI-invoking actions.

```jsx
<Button variant="primary">Save note</Button>
<Button variant="aiSubtle" icon={<i className="icon-sparkles" />}>Draft with MedAI</Button>
```

Variants: primary, secondary, ghost, destructive, ai, aiSubtle. Sizes: sm 28 / md 36 / lg 44.
