Modal dialog; footer takes right-aligned Buttons.

```jsx
<Dialog open title="Discard draft note?" footer={<><Button variant="ghost">Cancel</Button><Button variant="destructive">Discard</Button></>}>…</Dialog>
```
