Inline alert banner; `critical` = clinical safety, `ai` = AI notices.

```jsx
<Alert tone="critical" title="Allergy conflict" action={<Button size="sm" variant="destructive">Review order</Button>}>Amoxicillin conflicts with recorded penicillin allergy.</Alert>
```
