Content container; `ai` prop applies the violet AI-content treatment.

```jsx
<Card title="Active medications" action={<Button size="sm" variant="ghost">View all</Button>}>…</Card>
<Card ai title="Visit summary — drafted by MedAI">…</Card>
```
