/** Mandatory violet label on every piece of AI-generated content (EU AI Act transparency). */
export interface AIBadgeProps {
  label?: string;
  compact?: boolean;
}
export declare function AIBadge(props: AIBadgeProps): JSX.Element;
